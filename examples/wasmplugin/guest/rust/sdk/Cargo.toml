[package]
name = "llm-d-wasm-guest"
version = "0.1.0"
edition = "2021"
description = "Guest SDK for writing llm-d-router wasm plugins in Rust"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
